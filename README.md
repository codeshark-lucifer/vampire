#Setup Project:
> OpneGL(glad) graphic api
> Windows(win32) for window

> Utils (vec2, vec3, vec4, mat3, mat4, quat, color, logger)
> Mathf (max, min, clamp, lerp)
> Read/Write File
> Shader helper use template c++ for set uniform 
> Hello Triangles
> Batch Buffers > Intanced


> Game : 
    1. DrawSprite(Vec2 pos, DrawData data)
    2. DrawText(char* text, TextData data)
    3. GameDraw / UIDraw

> GameState:
    - Player
    - Transforms
    - UITransforms
    - Material
